import { RegisterForm } from "@/components/auth/RegisterForm";

export default function RegisterPage() {
  return <main className="w-full h-full bg-gray-800 flex justify-center items-center">
    <RegisterForm />
  </main>;
}