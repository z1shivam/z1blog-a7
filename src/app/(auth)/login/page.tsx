import { LoginForm } from "@/components/auth/LoginForm";

export default function LoginPage() {
  return (
    <main className="flex h-full w-full items-center justify-center bg-gray-800">
      <LoginForm />
    </main>
  );
}
